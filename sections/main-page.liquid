<section class="py-14">
  <div class="container-lux max-w-3xl">
    <h1 class="section-title text-center">{{ page.title }}</h1>
    <article class="mt-8 space-y-4 text-sm leading-relaxed text-ink/75">{{ page.content }}</article>
  </div>
</section>
{% schema %}{ "name": "Page" }{% endschema %}
